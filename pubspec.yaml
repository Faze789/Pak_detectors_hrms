name: hrms_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  provider: ^6.0.0
  firebase_messaging: ^16.1.1
  firebase_storage: ^13.1.0
  flutter_local_notifications: ^20.1.0
  shared_preferences: ^2.2.0
  intl: ^0.18.0
  pdf: ^3.10.8
  printing: ^5.12.0
  go_router: ^14.2.0
  file_picker: ^8.0.0
  path_provider: ^2.0.0
  firebase_core: ^4.4.0
  firebase_auth: ^6.1.4
  cloud_firestore: ^6.1.2
  geolocator: ^14.0.2
  geocoding: ^2.0.5
  url_launcher: ^6.3.2
  http: ^1.2.0
  flutter_web_plugins:        # ← ADDED: enables usePathUrlStrategy()
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true